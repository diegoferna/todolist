import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    checkBox: {
        width: 26,
        height: 26,
        justifyContent: 'center',
        borderColor: '#4EA8DE',
        borderWidth: 2,
        borderRadius: 100,
        marginLeft: 20,
    },

    check: {
        alignItems: 'center',
        alignSelf: 'center'
    },

    box: {}
})